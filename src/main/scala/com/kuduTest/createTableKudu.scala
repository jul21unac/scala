package com.kuduTest

class createTableKudu {
  /*
  def createKtable ( val  df : org.apache.spark.sql.DataFrame, val tableName: String, 
                    val kuduContext: org.apache.kudu.spark.kudu.KuduContext,
                    val primaryKey: List[a]): Unit = {
    // 1. Definir el schema
    val schema = df.schema



    val tableOptions = new org.apache.kudu.client.CreateTableOptions()
      .setNumReplicas(1)
      
    // 3. Crear la tabla si no existe
    if (!kuduContext.tableExists(tableName)) {
      kuduContext.createTable(tableName, schema, primaryKey, tableOptions)
      println(s"✅ Tabla '$tableName' creada correctamente")
    } else {
      println(s"⚠️  La tabla '$tableName' ya existe")
    }
  }
*/
}
